const Header = () => {
  return (
    <header className="App-header">
      <a className="Header-link Header-active">Home</a>
      <a className="Header-link">Movies</a>
    </header>
  );
};

export default Header;
